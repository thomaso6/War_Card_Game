var expect = chai.expect;

describe('functionTest', function(){
    describe(#shuffle(), function(){
        it('should shuffle the array randomly and return a new array', function(){
            var x = shuffle([1, 2, 3, 4,]);
            expect(x).to.equal(this.cards.sort(() => Math.random() - 0.5)
        });
    });
});
